{
  "name": "@nebula-cloud/control-plane",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "bootstrap": "bun src/bootstrap.ts",
    "smoke:console": "bun scripts/console-smoke.ts",
    "smoke:demo": "bun scripts/demo-smoke.ts",
    "reconcile:usage": "bun scripts/reconcile-usage.ts",
    "build": "bun build src/index.ts --target=bun --outdir=dist",
    "typecheck": "tsc -p tsconfig.json",
    "test": "bun test"
  },
  "dependencies": {
    "@nebula-cloud/auth": "workspace:*",
    "@nebula-cloud/contracts": "workspace:*",
    "@nebula-cloud/database": "workspace:*",
    "stripe": "^22.6.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "typescript": "~6.0.2"
  }
}
